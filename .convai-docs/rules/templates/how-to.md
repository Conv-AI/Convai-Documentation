---
title: Configure the API key
description: Add your Convai API key to the SDK settings so the Convai Unity SDK can authenticate its requests from inside a Unity project.
---

<!--
HOW-TO TEMPLATE. Reader is already competent; help them complete one specific task.
Replace all content. Keep 4-7 `##` sections. No body `#`. No `## Overview`. Sentence case headings.
Description: one outcome sentence, 120-160 chars. Lead paragraph is headingless.
Delete this comment before publishing.
-->

Add your Convai API key to the SDK settings so the SDK can authenticate requests. Use this page when
setting up a project for the first time or rotating an existing key.

## Prerequisites

- Convai Unity SDK installed
- A Convai account and a project API key from `<code class="expression">space.vars.dashboard_url</code>`
- <Add any other specific prerequisite, with inline-code values>

## <Do the task — base verb + object>

<Focused steps. Use a stepper for a short linear procedure, or `##`/`###` sections for longer ones.
Include a code block with the language specified when code is involved.>

{% hint style="warning" %}
<Warning that belongs before a risky action, if any. Max two hints on the page.>
{% endhint %}

## Verify the configuration

<State the observable success condition so the reader knows the task worked.>

{% hint style="success" %}
<Expected success state.>
{% endhint %}

## Troubleshooting

### <Symptom phrased as the reader sees it>

**Symptom:** <Exact UI text or console message in inline code.>

**Cause:** <Why it happens.>

**Fix:** <What to do.>

**Verify:** <How to confirm the fix worked.>

## Next steps

{% content-ref url="<related-page>.md" %}
[<Related page title>](<related-page>.md)
{% endcontent-ref %}
